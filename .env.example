# Copy this file to .env, then fill in your values.
# Required by the lead-capture CRM (api/ functions → Neon Postgres + HQ dashboard at /hq).
#
# Get DATABASE_URL from a Neon project: Dashboard → Connect → Connection string (https).
# Pick any strong password for ADMIN_TOKEN — it protects /hq and every /api endpoint.

DATABASE_URL=
ADMIN_TOKEN=